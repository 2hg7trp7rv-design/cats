# Cat's Tower — 成果物完成判定ゲート

更新日: 2026-08-22

この文書は、制作物が「作れた」だけで完成報告されることを防ぐための必須規則である。コード、画面、画像、仕様書、調査、QA、すべての成果物へ適用する。

## 1. 完了までの強制フロー

すべての作業を次の順序で行う。

1. **要求を定義する**
   - 何を作るかだけでなく、人間が何を期待しているか、参考対象のどこへ届くべきか、何を見たら失敗と感じるかを先に言語化する。
   - 機能要件、品質要件、感情要件、禁止事項、確認環境を分ける。
2. **成果物を制作する**
   - 要求を満たす最小単位を作り、途中で仕様矛盾を見つけた場合は制作を止める。
3. **制作者自身が成果物を検収する**
   - 出力された実物を、ユーザーが見るサイズ、端末、通常速度、通常導線で確認する。
   - 自動テスト結果やソースコードだけを見て合格にしない。
4. **反証する**
   - 「なぜこれは期待外れなのか」「参考対象より弱い箇所はどこか」「第三者が5秒見て何を誤解するか」を意図的に探す。
   - 最初の結論を守るための確認にしない。合格を覆す証拠を優先して探す。
5. **完成判定する**
   - 下記Gateがすべて合格した場合だけ`PASS`と報告する。
   - 一つでも不合格なら工程状態を`IN_PROGRESS`へ戻し、失敗原因と修正条件を記録して手順1から再構成する。
   - 権限、必要素材、ユーザー判断などで続行不能な場合だけ`BLOCKED`とし、未完成のまま`PASS`にしない。
6. **報告する**
   - 完成報告は、制作内容、検収方法、反証で見つけた問題、修正後の再検証、残る未確認を含める。
   - 「作成した」「buildが通った」「VercelがREADY」だけを完成根拠にしない。

## 2. 五つの完成Gate

| Gate | 問い | 必須証拠 |
|---|---|---|
| G1 要求適合 | 依頼された要件と禁止事項を満たしたか | 要求対照表、差分、未対応0。BLOCKEDがあれば総合PASS禁止 |
| G2 実物品質 | 最終成果物を実際に見て、破綻や仮物がないか | 最終サイズの画像・動画・runtime、全体一覧、代表状態 |
| G3 人間期待 | 専門外の依頼者が見ても期待水準へ届くか | 5秒理解、主役、魅力、感情、操作意欲の評価 |
| G4 比較・反証 | 強い参考対象と比べても重大な劣化を説明できないか | 横並び比較、弱点探索、代替案との比較 |
| G5 通常利用 | 実際の使い方で一連の体験が成立するか | 通常導線、失敗状態、再開、端末、必要な保存・性能QA |

G1だけの合格を「仕様PASS」、自動テストだけの合格を「技術QA PASS」と呼んでよいが、成果物全体を`PASS`とは呼ばない。

## 3. 制作者・批評者・判定者を分ける

重大成果物では、同じ視点のまま制作と採点を連続して行わない。

1. **制作者視点**: 要求に沿って作る。
2. **批評者視点**: 完成を前提にせず、欠点、期待差、参考対象との差を探す。
3. **判定者視点**: 制作量や努力を評価せず、ユーザーへ渡せる結果かだけで判定する。

ユーザー向けの完成報告を伴う成果物では、少なくとも一回、制作を担当していない批評者視点のレビューを必須とする。独立批評を実行できない場合は総合`PASS`にせず`BLOCKED`とする。批評結果を受け取っただけで終わらず、判定者が採否と修正を決め、修正後の実物を再確認する。

## 4. 成果物種別ごとのGate適用範囲

G1〜G5の具体的な証拠は成果物種別ごとに変える。制作開始前のAcceptance Matrixで適用範囲を固定し、完成できない都合で後から対象外にしない。

| 成果物 | G5「通常利用」の範囲 |
|---|---|
| 静止画・画面完成見本 | 最終サイズ、全画面一覧、画面遷移の理解、対象viewport、主要状態。未設計のmotion、音、保存、物理端末性能は対象外理由を先に固定する |
| 動きの絵コンテ | 入力、時間、キャンセル、通常motion、reduced-motion、音・視覚同期の因果 |
| 実装コード・runtime | 通常導線、失敗、再試行、保存、再読込、対象端末、性能、必要なdeployment |
| 仕様書・調査 | 想定利用者が判断・実行できること、反対資料、代替案、費用・運用上の現実 |

`N/A`はAcceptance Matrixで制作前に理由と代替証拠を固定した項目だけに使える。未対応、未確認、後工程送りを`N/A`へ変えて総合`PASS`にしない。未対応または`BLOCKED`が一件でも残る場合、総合`PASS`は禁止する。

## 5. デザイン成果物の追加Gate

デザイン、ゲーム画面、画像では次を必須にする。

- 書き出した全画面を個別表示とコンタクトシートの両方で目視する。
- 最終利用端末と同等のサイズで読む。拡大表示だけで合格にしない。
- 文字を隠しても、主役、目的、世界観、状態が伝わるか確認する。
- 5秒で「何の画面か」「次に何をしたいか」が分かるか確認する。
- 参考対象と、主役の大きさ、世界の占有率、情報密度、感情の強さ、独自性を横並びで比較する。
- 仮素材、空の図形、同じ背景の使い回し、説明文で代用した視覚表現を数える。完成見本では0を原則とする。
- 通常、押下、成功、失敗、最大報酬など、感情差が最も大きい状態を確認する。
- 座標が合うだけでなく、接地、遠近、陰影、解像感、材質、画風が同じ空間として見えるか確認する。
- 「整っている」だけでなく、「見たい・触りたい・続けたい」が成立するか判定する。

## 6. コード成果物の追加Gate

- 構文・型・自動テストに加え、通常ユーザー導線をruntimeで実行する。
- 正常系だけでなく、失敗、再試行、保存、再読込、戻る、端末幅、低速環境を確認する。
- 実装した機能がUIから到達でき、仮ボタンやQA専用APIだけで成立していないことを確認する。
- Vercelのdeployment成功と、対象ページの品質合格を分離する。
- コード差分を見て、要求外変更、重複、場当たり的上書き、将来の回帰原因を探す。

## 7. 調査・企画成果物の追加Gate

- 最初の結論へ反する資料や代替案を探す。
- 発見数、重複除去後、全文確認数、採用数、独立証拠系列を混同しない。
- 事実、推論、提案、未確認を分ける。
- 実行可能性、費用、時間、運用負荷、一般利用者の現実を確認する。
- 読みやすいだけの案ではなく、選ばなかった案より優れている理由を示す。

## 8. 証拠台帳と自動失効

成果物ごとに`quality-reviews/<artifact-id>/`を作り、次を保存する。

- 制作前に固定した`acceptance.json`
- 変更しない過去roundと、最新の`round-NNN.json`
- G1〜G5の個別判定、証拠パス、批評者、失敗原因、再利用範囲、次の仮説
- 対象成果物すべてのSHA-256
- Acceptance Matrix自体のSHA-256
- 制作者、各独立批評者、最終判定者ごとのattestationファイルとSHA-256

過去roundは上書きせず、新しいroundを追加する。`PROJECT_STATUS.json`は最新reviewを一つだけ指す。

`tests/quality-gate.mjs`は次を拒否する。

- 成果物ハッシュまたはAcceptance Matrixハッシュがreview後に変わった。
- 独立批評者、G1〜G5、証拠、round番号が欠けている。
- 一つでもGateが不合格なのに工程状態が`PASS`である。
- 総合`PASS`なのに未対応、`BLOCKED`、重大指摘、仮素材が残る。
- `PROJECT_STATUS.json`と最新reviewの工程状態が一致しない。
- PR差分の成果物がAcceptance Matrixの`ownedPaths`へ登録されていない。
- `ownedPaths`の変更ファイルが最新reviewの完全なhash manifestへ含まれていない。
- base branchに存在したAcceptance Matrixまたは過去roundを変更、削除、改名して履歴を上書きする。
- PRで変更した成果物の最新reviewが総合`PASS`ではない。
- `infrastructurePaths`を固定allowlistから拡大して成果物を検査対象外にする。
- 制作者、独立批評者、最終判定者のattestationが欠けるか、対象Acceptance／成果物setのhashと一致しない。
- PASS対象のAcceptanceが、review対象sourceより前のcommitで固定されたことを証明できない。

GitHub Actionsはファイル種別による除外を設けず、すべてのPull Requestと`main` pushでこの検査を実行する。対象reviewが総合`PASS`でないPRはDraftのままにし、品質Gate合格前にReady化・mergeしない。保護設定を迂回して`main`へ入った場合もpush側の検査を不合格にする。

品質Gate本体、workflow、registry、review履歴には`CODEOWNERS`を設定する。ただしCODEOWNERSだけではmergeを強制停止できない。GitHub側でPull Request必須、`vertical-tower-qa`必須、direct pushとforce push禁止、管理者bypass制限を確認できるまでは、`tamperResistantMergeEnforcementVerified`を`false`のままにし、「仕組み上絶対に迂回できない」と報告しない。

## 9. 不合格時の再構成記録

不合格を見つけた場合は、次を残す。

1. 何を完成と誤認したか。
2. どの確認を省略したか。
3. ユーザー期待と何がずれたか。
4. 根本原因は要求、順序、素材、実装、QAのどこか。
5. 次回の合格条件へ何を追加するか。
6. どこまで再利用し、どこから作り直すか。

同じ成果物を小手先で整えて再提出せず、根本原因に応じて構成、工程順、素材、評価基準から戻す。

## 10. ユーザーへ確認を戻す条件

通常の欠陥発見、比較、修正、再検証は制作者側の責任で行い、ユーザーへ毎回の検品を要求しない。制作途中のものを「完成したので確認してください」と渡さない。

ユーザーへ判断を求めるのは次の場合に限る。

- 同等に成立する複数方向のうち、好みで製品方針が大きく変わる。
- 既存の承認済み参考物から好みを合理的に推定できない。
- 必須素材、権限、実機、外部サービス上の操作がユーザーにしか用意できない。
- 破壊的操作、公開、課金、送信など、明示的な承認が必要である。

確認が必要な場合も、白紙の質問を返さず、検証済みの推奨案、代替案、差、推奨理由を提示する。確認が不要な問題は内部で修正を続け、G1〜G5合格後にまとめて報告する。

ユーザーが完成品質を否認した場合は、説明して旧判定を守らない。即時に総合`PASS`を撤回し、工程を`IN_PROGRESS`へ戻し、新しい反証roundを追加する。否認内容をAcceptance Matrixと次roundの失敗条件へ反映する。

## 11. 工程4で発生した誤判定

2026-08-22の9画面成果物は、画面数、viewport、44px操作領域、overflow、console errorに合格したため、完成見本として誤って`PASS`と判定した。

省略した確認:

- 最終画像を本番美術として批評すること
- 商人サーガ級の見た瞬間の魅力との比較
- 猫、店、塔、報酬が文字ではなく絵で伝わるかの確認
- 階とキャラクターが同じ空間へ接地して見えるかの確認
- アートバイブルと試験素材より先に完成見本を作る工程矛盾の確認

訂正:

- 9画面の構造・状態網羅・レスポンシブ技術QA: `PASS`
- 本番静止美術、ゲーム内容の可読性、感情設計の完成品質: `FAIL`
- 動きと触り心地: 工程5の判定対象であり、工程4では`NOT_STARTED`
- 工程4全体: `IN_PROGRESS`
- 現在の成果物: 完成見本ではなく再制作に使う構造ワイヤーフレーム
- PR #3: 工程4全体が五つのGateへ合格するまでマージしない

## 12. 完成報告テンプレート

完成報告には最低限、次を含める。

1. 作ったもの
2. G1〜G5の判定と証拠
3. 制作後の自己検収で発見した問題
4. その問題へ行った再構成・修正
5. 再検証結果
6. 未確認・物理的制約
7. branch、commit、PR、Vercel URL
8. `PASS`、`IN_PROGRESS`、`BLOCKED`の最終判定

G1〜G5の一つでも不合格なら、「完成しました」から報告を始めない。
